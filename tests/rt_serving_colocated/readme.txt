Colocated serving regression cases.

This suite validates the new colocated runtime for:
- serial-equivalent single-request behavior,
- batched prefill across simultaneous requests,
- batched decode iterations across simultaneous requests.
